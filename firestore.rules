rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All football data is public read-only
    // Writes are exclusively via Admin SDK (server-side), which bypasses these rules
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
